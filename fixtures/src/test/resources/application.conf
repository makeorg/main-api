feeder {
  users = "feeder/users.csv"
  proposals = "feeder/proposals.csv"
  users-vff = "feeder/users_vff.csv"
  proposals-vff = "feeder/proposals_vff.csv"
}

service {

  # Url to service host:
  host = "http://localhost:9000"
  host = ${?API_HOST}

  # Endpoints base path:
  api-link = "/"
  api-link = ${?API_ENDPOINT_BASE_PATH}
}

default-admin {
  first-name: "admin"
  first-name: ${?APP_ADMIN_DEFAULT_FIRST_NAME}
  email: "admin@make.org"
  email: ${?APP_ADMIN_DEFAULT_EMAIL}
  password: "admin"
  password: ${?APP_ADMIN_DEFAULT_PASSWORD}
}

